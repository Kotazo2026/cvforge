import type { RefObject } from 'react';

/** Placeholder — implémentation complète au Bloc 9. */
export async function exportToPDF(
  templateRef: RefObject<HTMLDivElement | null>,
  filename: string,
): Promise<void> {
  void templateRef;
  void filename;
  throw new Error('Export PDF non implémenté — voir Bloc 9');
}
